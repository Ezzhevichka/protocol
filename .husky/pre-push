#!/bin/sh

branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ]; then
  echo "Push to main is blocked. Use a feature branch and PR."
  exit 1
fi

if [ "$branch" = "master" ]; then
  echo "Push to master is blocked. Use a feature branch and PR."
  exit 1
fi

exit 0